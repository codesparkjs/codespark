---
title: Usage
description: Learn how to use the Codespark component in your React application.
---

import CustomizeCodespark from '../../app/routes/examples/customize-codespark/embedded/App.tsx';

## Basic Usage

Pass your code as a string to the `Codespark` component:

```tsx preview
import { Codespark } from '@codespark/react';

const code = `export default function App() {
  return (
    <div className="text-center">
      <h1 className="text-xl font-bold">Hello World</h1>
      <p>This is my first Codes Park!</p>
    </div>
  );
}`;

export default function App() {
  return <Codespark code={code} />;
}
```

```tsx codespark
export default function App() {
  return (
    <div className="text-center">
      <h1 className="text-xl font-bold">Hello World</h1>
      <p>This is my first Codes Park!</p>
    </div>
  );
}
```

## Customize Your Own

Codespark features a modular architecture with composable components. Mix and match `CodesparkEditor`, `CodesparkPreview`, and other building blocks to create your own custom layout. [Try in playground](/playground?boilerplate=customize-codespark).

```tsx
import { CodesparkEditor, CodesparkPreview, CodesparkProvider } from '@codespark/react';
```

```tsx
<CodesparkProvider>
  <CodesparkEditor />
  <CodesparkPreview />
</CodesparkProvider>
```

<CustomizeCodespark />
